<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="amtt.epam.com.amtt">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".CoreApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".app.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="7f897d0aab2e8ae13009c36be12894a22ad6f168" />
        <activity
            android:name=".app.SecondActivity"
            android:label="@string/app_name"/>

        <activity
            android:name=".app.ThirdActivity"
            android:label="@string/app_name"/>

        <service
            android:name=".service.TopButtonService"
            android:exported="false"/>

        <provider
            android:name=".contentprovider.AmttContentProvider"
            android:authorities="amtt.epam.com.amtt.contentprovider"
            android:exported="false">
        </provider>

    </application>


</manifest>
